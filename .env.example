# Example Configuration for Homey Flow Architect

# Path or command to launch your mcp-homey server
MCP_SERVER_COMMAND=npx
MCP_SERVER_ARGS=-y,@homey/mcp-server

# Note: The actual arguments might depend on how your mcp-homey server is set up.
# If it's a globally installed package or a local node script, specify it appropriately.
